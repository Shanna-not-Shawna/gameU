<div class="login-container">
  <!-- Login form and other content go here -->
  <img src="./assets/neon1.jpg" class="background-image" alt="Background Image">
</div>

</div>
<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">GameU</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/createAccount">Create Account</a>
        </li>
        <li class="nav-item">
            {{#if logged_in}}
            <a class="nav-link" href="/profile">Profile</a> 
            </li>
            <li>
            <button class="no-button btn btn-dark" id="logout">Logout</button>
            {{else}}
            <a class="nav-link" href="/login">Login</a>
            {{/if}}
          </li>

      </ul>

    </div>
  </div>
</nav>
<main class="modal-containerLg modal-header container">
   <div class='modal-dialog'>
    <div class="welcome-sign">
   <h2 class='modal-title mb-3'>WELCOME BACK </h2>
   <h4 class='modal-title mb-3'>I missed you 👁️👁️</h4>
    </div>

  <form class="modal-content login-form">
    <div class="modal-body">
      <div class="form-group">
        <label for="email">email:</label>
        <input type="text" class="form-control" id="email-login" placeholder="Enter your email" required />
      </div>
      <div>
      <div class="form-group">
        <label for="password">Password:</label>
        <input type="password" class="form-control" id="password-login" name="password" autocomplete="on"
          placeholder="Enter your password" required />
        <button type="submit" class="btn btn-primary">Login</button>
      </div>
      </div>
    </div>
  </form>
</main>
  <script src="/js/login.js"></script>
